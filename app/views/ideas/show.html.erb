<!-- Provides Idea information -->
<%= render 'idea', idea: @idea %>

<!-- Options for Idea modification or navigation -->
<% if @idea.user == current_user %>
  <%= link_to 'Edit', edit_idea_path %> |
<% end %>
<%= link_to 'Back', ideas_path %>

<br><br>

<!-- Comments Section -->
<%= render 'comments/comment' %>

<!-- Google Map -->
<%= render 'map' %>

<!-- Hotwire -->
<%= render 'hotwire' %>